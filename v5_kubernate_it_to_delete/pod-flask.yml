apiVersion: v1
kind: Pod
metadata:
  name: flask-pod
  labels:
    zone: prod
    version: v1
    app: web
spec:
  containers:
  - name: ping-flask
    image: natanbs/flask-ping:latest
    ports:
    - containerPort: 5000
